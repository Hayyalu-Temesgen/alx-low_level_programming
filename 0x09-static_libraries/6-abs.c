#include "main.h"

/**
 * _abs - this function will compute the ablsolute value
 * @n: the integer to be checked
 * Return: will return 0 if successful
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

