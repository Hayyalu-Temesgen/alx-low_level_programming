#include "main.h"

/**
 * _isdigit - the condition that checks the digit
 * @c: the intiger that will be checked
 * Return: if successful will return 1, fail will be 0
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

