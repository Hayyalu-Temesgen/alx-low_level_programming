#include "main.h"

/**
 * _islower - this function will check for lowercase
 * @c: the character to be checked
 * Return: will return 1 if successful
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

